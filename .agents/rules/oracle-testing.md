# Oracle Testing Access

When performing Oracle VM testing for BlackBread:

1. Open WSL: work from `/mnt/d/BLACKBREAD`
2. SSH: `ssh oracle-blackbread`
3. Navigate: `cd ~/blackbread`

Always use this exact sequence. Never expose hostnames, IP addresses, SSH key paths, passwords, connection URLs, or secrets in outputs or artifacts.
